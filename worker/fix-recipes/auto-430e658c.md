# auto-430e658c
## Diagnosis
`review.cjs` silently catches a `require('../../worker/codex-reviewer.js')` failure (line 1060), leaving `codexReviewer = undefined`. The branch at line 1306 (`else if (codeReviewer)`) then calls the legacy `codeReviewer.reviewCode()` with no key guard. Because `OPENAI_API_KEY` was intentionally removed from both `.env` and `worker/.env` on 2026-04-16 (to avoid conflicting with Codex ChatGPT auth mode), `code-reviewer.js:594` immediately throws `MODEL_FATAL: no OPENAI_API_KEY configured`. This hits every task in the affected process run because the module-level `_codexPreflightResult` cache in `codex-reviewer.js` never gets a chance to initialize — the reviewer never loads at all.

## Root Cause
`engine/stages/review.cjs:1306` — `else if (codeReviewer)` has no guard for missing `OPENAI_API_KEY`; the branch is entered whenever `codexReviewer` fails to load, immediately cascading into `code-reviewer.js:594`'s fatal throw.

## Fix
In `engine/stages/review.cjs`, add `hasLegacyReviewerApiKey()` as a conjunct on the legacy-reviewer branch, and update the final `else` message to be actionable: