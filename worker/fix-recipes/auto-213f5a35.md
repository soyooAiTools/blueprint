# auto-213f5a35
## Diagnosis
`adapters/schema/validate-schema.cjs` maps Ajv validation errors to strings using `e.instancePath`, the property introduced in Ajv v8. The installed Ajv package is **v6.12.6**, whose error objects carry `e.dataPath` instead. Reading a non-existent property yields `undefined`, so every error entry in the array becomes `"undefined should NOT have additional properties"` or `"undefined should have required properties"`, making the error message completely opaque to both the retry prompt and the engineer. All three repeated instances in the fingerprint are separate Ajv errors on the same JSON blob, each rendered identically as `undefined …`.

## Root Cause
`adapters/schema/validate-schema.cjs:19` — `e.instancePath` is `undefined` under Ajv v6.12.6 (which uses `e.dataPath`).

## Fix
In `/opt/blueprint-editor/adapters/schema/validate-schema.cjs`, change **line 19** from: