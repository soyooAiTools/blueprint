# auto-68029911
## Diagnosis
When `fidelity-contract-synthesize` skips (source HTML is off-contract — missing `PHASES`, `ENTITY_STYLE`, or `SCENE_CONFIG` top-level JS literals), `ctx.blueprint.fidelityContract` is never set. `fidelity-contract-produce` then also skips (no base contract resolvable). `fidelity-source-diff.canSkip()` only checks for `ctx.sourceHtmlPath` and the env flag, so it proceeds — but `resolveFieldDiffTemplate()` finds no in-memory contract and no `ctx.fidelityContractPath`, silently falling through to `DEFAULT_CONTRACT_PATH`: the `space-ranger-v0.5` fixture. The fixture is from a completely unrelated game, producing 26+ false-positive blocking entity diffs (space-ranger entities absent from the real build). The `~100%` pixel divergence is also a direct consequence: the comment in `fidelity-contract-synthesize.cjs` (lines 15–18) explicitly documents "The 99% pixel-diff is the fixture mismatch, not the Luna build" — the wrong overlay contract data injected via `buildVisualAssetsForRequest` causes the target build to render with mismatched entity/scene parameters, making the screenshot nearly entirely different from the source HTML render.

## Root Cause
`engine/stages/fidelity-source-diff.cjs:162` — `canSkip()` does not check whether a task-specific contract is actually available before returning `false`. When neither `ctx.blueprint.fidelityContract` nor `ctx.fidelityContractPath` is set, the stage executes and `resolveFieldDiffTemplate()` (line 358) falls back to `DEFAULT_CONTRACT_PATH` (line 371–378): the space-ranger-v0.5 fixture hardcoded at `work/task25-sam-delivery-verify/…/fidelityContract.json`.

## Fix
In `engine/stages/fidelity-source-diff.cjs`, extend `canSkip()` to return `true` when no task-specific contract is available. Insert the following block **after** the `SKIP_FIDELITY_SOURCE_DIFF` check (after line 173, before the final `return false`):