# auto-289f827a
## Diagnosis
This is a field-diff-only `fidelity-source-diff` block: exactly one blocking diff in `phase1`, 36 advisory diffs, and no pixel-gate failure. The recurring blocker is a `scene.backgroundColor` mismatch, which is color-only and conflicts with the stage's structural, color-insensitive pixel policy.
## Root Cause
engine/stages/lib/field-diff.cjs:619 marks `scene.backgroundColor` mismatches as blocking.
## Fix
In `engine/stages/fidelity-source-diff.cjs`, inside `demoteAdvisoryBuckets(fieldDiffs, targetFields)`, add this narrow demotion before the `demote === true || demote === false` copy block:

    // scene.backgroundColor is a color-only scene diff. When the structural
    // pixel gate is clean, this field mismatch should not hard-block fidelity.
    if (cat === 'scene-mismatch' && String(d.path || '') === 'scene.backgroundColor') demote = true;

Keep `scene-missing` and all other `scene-mismatch` paths blocking.
## Verification
cd /opt/blueprint-editor && node -e "const stage=require('./engine/stages/fidelity-source-diff.cjs'); const diffs=stage._internals.demoteAdvisoryBuckets([{category:'scene-mismatch',path:'scene.backgroundColor',blocking:true},{category:'entity-missing',path:'entities.Player',blocking:true}],{entityDetails:{}}); if(diffs[0].blocking!==false||diffs[0].bucketPolicy!=='plan-c-advisory') throw new Error('scene background was not demoted'); if(diffs[1].blocking!==true) throw new Error('non-scene diff was unexpectedly demoted'); console.log('ok');"