# auto-09618e16
## Diagnosis
The `review` stage runs `checkConformance()` before the fix loop and correctly identifies missing `AddCompletedPhase` calls as critical issues, but pushes them only to `ctx.blueprint.feedbackHistory`. In round 1, the codex reviewer receives only `reviewedCode` + `extraFiles` — it does not see `feedbackHistory` — so if the generated code is otherwise syntactically clean, the reviewer returns `passed: true`. The `reviewResult.passed` branch (line 166 of `review.cjs`) immediately returns `{ done: true }` without re-running the phase-coverage check, silently discarding all 11 conformance violations. The post-loop coverage gate then throws `Phase coverage too low: 0/11 (0%)` after the entire retry budget has been consumed.

## Root Cause
`engine/stages/review.cjs:166` — the `if (reviewResult.passed)` branch returns `{ done: true }` without re-validating spec conformance, so a reviewer PASS on code that contains zero `AddCompletedPhase` calls propagates cleanly through the loop and explodes at the post-loop gate.

## Fix
In `engine/stages/review.cjs`, insert a mid-loop conformance gate **between** the `if (reviewResult.passed) {` test (line 166) and the `ctx.addLog('review', reviewerName + ' review PASSED'...)` call (line 167). Add the following block: