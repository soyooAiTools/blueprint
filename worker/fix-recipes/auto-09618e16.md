# auto-09618e16
## Diagnosis
The review stage fix loop updates `reviewedCode` in memory via `recode()`, but `recode()` writes its output to a **temporary directory** (e.g. `/tmp/linux-reviewfix-<taskId>-<round>`), never back to `ctx.workDir`. At the end of the fix loop, `loop.run(ctx).then()` re-reads `GameFlowManagerMain.cs` from `ctx.workDir` — which is still the stale original codegen file — and overwrites `ctx.csCode` with it. The phase coverage gate immediately following then compares the 11 spec `phaseId` values against this stale code and finds 0 matches, throwing a hard failure even though `reviewedCode` (which the reviewer actually approved) contained all phases correctly.

## Root Cause
`engine/stages/review.cjs` **lines 332–344** — the `loop.run(ctx).then()` block discards `reviewedCode` and re-reads `ctx.csCode` from the unmodified `ctx.workDir` disk file before the phase coverage gate runs.