# auto-09618e16
## Diagnosis
The `review` stage has a post-loop phase coverage gate (line 558) that throws `Phase coverage too low: 0/N (0%)` after `loop.run()` resolves. The gate is only reachable when the loop exits with `done: true` (LLM reviewer passed). The original in-loop pre-check only tested `preCheckPhaseIds.length === 0` (any `AddCompletedPhase` call exists), so code that calls `AddCompletedPhase("phase_1")` through `AddCompletedPhase("phase_11")` cleared the guard, received a passing LLM review (reviewers do not validate phaseId strings against the spec), and then failed the semantically-aware post-loop gate with zero retries remaining. The fix replaces the weak existence guard with the same 3-level fuzzy coverage logic used in the post-loop gate, so any coverage < 80% forces a recode while retries are still available.

## Root Cause
`engine/stages/review.cjs:558` — post-loop `throw new Error('Phase coverage too low: ...')` reachable because the in-loop pre-check (pre-fix) only tested `preCheckPhaseIds.length === 0`, allowing code with generic phase names (e.g. `"phase_1"` … `"phase_11"`) to pass silently, receive a passing LLM review, and then fail the post-loop semantic coverage check with no retries remaining.

## Fix
In `engine/stages/review.cjs`, inside `attempt: function(ctx, round, maxRounds)`, replace the weak pre-check block (which only tested for zero AddCompletedPhase calls) with the 3-level fuzzy coverage pre-check. The corrected block must be inserted after the `staticCheck` block and before the LLM reviewer invocation: