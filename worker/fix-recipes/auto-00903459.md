# auto-00903459
## Diagnosis
The runner correctly detects a fatal auth/quota condition, but the reported error body is selected from the whole stdout/stderr stream. Codex CLI prints `Reading prompt from stdin...` before the real failure line, so the pipeline fingerprint captures the startup banner instead of the actionable quota/auth text.
## Root Cause
worker/codex-code-coder.js:93
## Fix
In `worker/codex-code-coder.js`, replace `resolveRunnerErrorBody()` with a line-level selector and use/export it: