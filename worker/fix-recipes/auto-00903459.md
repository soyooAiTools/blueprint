# auto-00903459
## Diagnosis
When the Codex CLI exits early (< 10 s, exit 1) due to an auth/quota rejection, both `runClaudeCode` (line 553) and `runCodexExecCode` (line 741) compute `baseErr` as `stdout || stderr || fallback`. Because stdout is non-empty (it contains the Codex startup banner `"Reading prompt from stdin...\nOpenAI Codex v0.125.0 (research preview)\n--------\nworkdir: ..."`) it wins over stderr even though stderr holds the actual authentication error. The MODEL_FATAL classification is correct (detected via the combined `streams`), but the embedded diagnostic text is the useless startup banner instead of the real auth failure reason. This makes post-mortem triage nearly impossible and also means the cooldown file written in `generateWithCodex` records the banner text rather than the true cause.

## Root Cause
`worker/codex-code-coder.js:553` (inside `runClaudeCode` early-exit guard)  
`worker/codex-code-coder.js:741` (inside `runCodexExecCode` early-exit guard)

Both lines read: