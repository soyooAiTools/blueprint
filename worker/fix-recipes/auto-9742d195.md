# auto-9742d195
## Diagnosis
`_buildStuckDiagnosis()` in `cua-verify.cjs` unconditionally set `rootCause = 'visual_freeze'` whenever the CUA issue text matched `VISUAL_FREEZE_PHRASES`, regardless of whether any phases had already completed. When phase `defeatFirstAliens` was done but `buildTurretDefense` was unreachable, the VLM legitimately reported "no meaningful visual change" (the screen IS static waiting for the transition) — but the real root cause was a broken phase-transition condition, not a global rendering freeze. The wrong `visual_freeze` label injected incorrect batch-firing/autoPlay-gate advice into Claude's recode prompt and simultaneously armed the `visual_freeze` fast-escalation path (`_noProgressRounds >= 2` → full regen, then bare `else { throw }` at `_noProgressRounds = 3`), producing FATAL after only 3 rounds with zero chance for phase-transition-specific fixes to be attempted.

## Root Cause
`engine/stages/cua-verify.cjs` — `_buildStuckDiagnosis()`, `hasVisualFreezePhrase` branch (line 102 in current file).

The unfixed line was: