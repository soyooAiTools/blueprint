# auto-90f7084e

## Diagnosis
The `fidelity-source-diff` stage throws a hard BLOCK when phase1 produces 37 phantom entity-missing field diffs from a truly-black unrendered WebGL canvas (99.97% pixel divergence). The canvas-black demote guard (`auto-3db28275`/`auto-f23a1abb`) is supposed to convert this hard-block to advisory so the `visual-check` recode loop can recover, but it uses `blockingFieldDiffPhases.every(…)` — requiring **all** phases with blocking field-diffs to exceed the 95% black-canvas demote threshold. Phases 2–4 also carry blocking field-level diffs produced by cross-engine renderer variance (Three.js → PlayCanvas: different shaders, AA, lighting) and land in the 60–90% pixel-diff band, which is below 95%. The `.every()` predicate therefore evaluates `false`, the guard is bypassed, and the hard-block is thrown every time. With `canRetry: false` on the stage, the task enters a permanent fail loop across all 4 pipeline retries without the visual-check recode loop ever running.

## Root Cause
`engine/stages/fidelity-source-diff.cjs:380` — `.every()` in the `allPhasesBlack` predicate is too strict. When phase1 shows a genuine initialization-failure black canvas (99%+) but phases 2–4 have cross-engine-variance blocking diffs at 60–90%, `allPhasesBlack` is always `false`.