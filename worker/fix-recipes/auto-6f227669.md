# auto-6f227669
## Diagnosis
When the Python CUA agent reports "Screenshot sharing: N spec phases share only 1 screenshot(s)", `worker-playableagent.js` emits the issue as `[visual-quality]` (old) or `[screenshot-timing]` (new). In `_buildStuckDiagnosis`, `allIssueText` contains "screenshot sharing" but no entry in `VISUAL_FREEZE_PHRASES` matches, so `rootCause` falls through to `'unknown'`. Because `rootCause !== 'visual_freeze'`, the fast-escalation block (`_noProgressRounds >= 2` → full-regen) never fires. The only backstop is the fingerprint circuit-breaker, which throws FATAL at 3 repeats without ever having attempted a full regen — leaving Claude no chance to fix the batch-firing root cause.

## Root Cause
`engine/stages/cua-verify.cjs:55-63` — `VISUAL_FREEZE_PHRASES` array missing `'screenshot sharing'`; and `engine/stages/cua-verify.cjs:514` — FP-breaker FATAL condition missing an exemption analogous to `isPhaseSkippedFp` for this fingerprint class.

## Fix

### Change 1 — add `'screenshot sharing'` to `VISUAL_FREEZE_PHRASES` (~line 58)