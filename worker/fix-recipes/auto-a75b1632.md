# auto-a75b1632
## Diagnosis
This is backend availability handling, not a visual rendering failure. `visual-check` fails after its repair path calls `recode()`, which delegates through `worker/codex-coder.js` into the Codex code runner. The active config sets `BLUEPRINT_DISABLE_CLAUDE=1`, so a persisted Codex primary cooldown should not attempt or require a Claude fallback.
## Root Cause
worker/codex-code-coder.js:1569
## Fix
In `worker/codex-code-coder.js`, change the active cooldown branch from unconditional fallback-or-fail behavior to a guard that only routes to `claude-code` when Claude is enabled: