# auto-2077d92c
## Diagnosis
The repeated fingerprint is a CUA visual-freeze signal from observer/autoplay screenshots staying static. `cua-verify` already has dedicated no-progress handling for `visual_freeze`, including stuck diagnosis and full-regeneration escalation. The generic identical-fingerprint circuit breaker runs before that path can finish, so round 3 throws `Fingerprint repeat FATAL` instead of letting visual-freeze recovery own the failure.
## Root Cause
engine/stages/cua-verify.cjs:575 - `_fpRepeatCount >= FP_REPEAT_FATAL_AT` applies to `[visual-freeze]` fingerprints even though visual-freeze has dedicated `_noProgressRounds` escalation at engine/stages/cua-verify.cjs:684.
## Fix
In `engine/stages/cua-verify.cjs`, add a local predicate near the other helper predicates: