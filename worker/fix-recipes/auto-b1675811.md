# auto-b1675811
## Diagnosis
When `completedPhases.length === 0` and `noProgressRounds >= 2`, `_buildStuckDiagnosis` (line 67–68) falls through the `noPhasesCompleted` fallback and sets `rootCause = 'visual_freeze'`. The `rootCauseAdvice.visual_freeze` text (lines 108–113) then instructs the AI regen to fix **batch-firing** (phases completing too fast, 20f gate), which is the opposite of the actual problem — the game never initialized at all (Camera/Canvas/Start() failure). The AI regen faithfully follows the wrong advice, adds anti-batch-firing guards to already-broken initialization code, produces no phase progress, and the FATAL is thrown again in round 3. This repeats across every affected task (12 retries / 3 tasks).

## Root Cause
`engine/stages/cua-verify.cjs:67-68` — `noPhasesCompleted` (zero phases ever completed) is mapped to the same `visual_freeze` root cause as batch-firing (phases complete but screen looks static). The advice lookup at line 108–113 (`rootCauseAdvice.visual_freeze`) then gives the AI entirely wrong guidance for the init-failure sub-case.

## Fix
Three changes, all in `engine/stages/cua-verify.cjs`:

### Change 1 — Split rootCause classification (lines 67–68)