# auto-b1675811
## Diagnosis
When no phases complete across CUA rounds, `_buildStuckDiagnosis()` collapses the case into `rootCause = 'visual_freeze'` at line 67 and feeds the LLM `rootCauseAdvice.visual_freeze` — which opens with *"Game screen is visually static **despite phases completing**. Phases are completing too fast (batch-firing)"*. The LLM receives factually wrong context (phases are NOT completing; none have ever fired) and generates a full-regen that still cannot start the game. Round 3 hits the FATAL throw at line 370, the task is retried externally, and the same 3-round pattern burns again — producing the "12 retries across 3 tasks" fingerprint. A secondary waste: round 1 still fires a surgical `patchRecode` even though initialization-level failures can never be fixed by patching specific lines.

## Root Cause
`engine/stages/cua-verify.cjs:67` — the `noPhasesCompleted && noProgressRounds >= 2` branch maps into `visual_freeze` (a batch-firing concept) instead of a dedicated `game_never_started` cause, and `engine/stages/cua-verify.cjs:108` — the `rootCauseAdvice.visual_freeze` string explicitly says "despite phases completing / batch-firing" which is wrong when `completedPhases.length === 0`.

## Fix

### 1. Split `game_never_started` out of `visual_freeze` (line 67)