# auto-b1675811
## Diagnosis
When `completedPhases.length === 0` ("none completed"), `_buildStuckDiagnosis` only promotes `rootCause` to `visual_freeze` after **two** no-progress rounds (`noPhasesCompleted && noProgressRounds >= 2`). During round 1 of no-progress, rootCause falls through to `phase_transition_broken`, `runtime_error`, or `unknown`, so the single recode that executes before the round-2 FATAL carries **generic or batch-firing advice** — completely wrong for a game that never initialized. The batch-firing `visual_freeze` advice (`rootCauseAdvice['visual_freeze']`) explicitly describes phases completing too fast with a 20-frame autoPlay gate, but the real problem is that `Start()` never moved any objects out of the pool or that a silent `NullReferenceException` killed initialization before phase 1 could fire. The one recode attempt fixes the wrong thing, the FATAL fires on round 2, and the pattern repeats on every subsequent task retry — explaining the 12 hits across 3 tasks.

## Root Cause
`engine/stages/cua-verify.cjs:67` — rootCause detection for the `noPhasesCompleted` branch: