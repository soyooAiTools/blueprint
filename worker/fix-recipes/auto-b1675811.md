# auto-b1675811
## Diagnosis
`_buildStuckDiagnosis()` uses two independent sources to detect "no phases completed": `completedPhases.length === 0` (from `consolePhaseCoverage`, instrumented and reliable) and `stuckAtPhase <= 0` (numeric parse from CUA issue text, unreliable). The original `&&` (AND) condition required **both** to be true simultaneously. When the VLM includes a fragment like `"[phase-coverage] 1/5"` in its issue text, the numeric parser yields `stuckAtPhase = 1`, making `stuckAtPhase <= 0 = false` — so `noPhasesCompleted = false` even though zero real `AddCompletedPhase` calls ever fired. The `codegen_init_failure` guard (line 78) is skipped, control falls through to the `visual_freeze` keyword check, and because the VLM also uses phrases like "no phase progression" or "essentially unchanged", `rootCause` is set to `visual_freeze`. After 2+ no-progress rounds the FATAL throw fires; `error-classifier` maps `/Visual freeze FATAL/i` to `CUA_FATAL_PATTERNS → FATAL`, permanently terminating tasks that should instead receive a retryable full-regen attempt via the `codegen_init_failure → CODE` path. This accounts for 12 spurious FATAL terminations across 3 tasks.

## Root Cause
`engine/stages/cua-verify.cjs:76` — `noPhasesCompleted` AND-condition disagrees with `consolePhaseCoverage` when VLM issue text carries a phase-count fragment, suppressing `codegen_init_failure` classification and allowing `visual_freeze` FATAL to fire on genuinely recoverable zero-progress cases.

## Fix
In `engine/stages/cua-verify.cjs`, change the `&&` (AND) to `||` (OR) in the `noPhasesCompleted` assignment so that `consolePhaseCoverage` (the reliable instrumented source) alone is sufficient to trigger `codegen_init_failure`: