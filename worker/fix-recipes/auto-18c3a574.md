# auto-18c3a574
## Diagnosis
The report is field-diff-only: pixel gate is clean, but phase2 target HUD/visibility matches phase3 (`Phase 3/5`, phase3 guide text, phase3 entity set). That means rendering is healthy and the diff is caused by the Luna bridge phase driver letting runtime progression overrun the requested fidelity phase.
## Root Cause
`worker/linux-bridge-build.js:3986` — `driveLoopComponentToPhase()` calls `Phase_<id>_Init()` and then `UpdateGameState()` without forcing the fidelity phase visibility and gate snapshot for the requested phase before extraction.
## Fix
In `worker/linux-bridge-build.js`, inside `driveLoopComponentToPhase(loopComp, phaseNumber)`, immediately after `if (typeof loopComp[initName] === "function") loopComp[initName]();` add: