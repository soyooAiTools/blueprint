# auto-04f18491
## Diagnosis
When the W1b 5-partial skeleton split is active (`skeletonResult.mode === 'w1b-5partial'`), `generateSkeleton` places `TODO_PHASE_N_INIT_START/END` markers inside `Phase_<id>_Init()` methods that live exclusively in the Flow partial (`skeletonResult.flow`). However, `codegen-schema.cjs` only calls `templateEngine.fillSkeleton` on `skeletonResult.main`, which contains no phase-init markers. `replaceAllTodos` correctly surfaces every `TODO_PHASE_N_INIT` key as missing, and the stage throws before the Flow file is even written to `ctx.extraFiles`. The Flow partial is then stored raw (unfilled) from `skeletonResult.flow`.

## Root Cause
`engine/stages/codegen-schema.cjs:44-46` — `fillSkeleton` is called with `skeletonResult.main` only; missing-marker check does not account for the W1b case where phase-init markers are in the sibling Flow file. `engine/stages/codegen-schema.cjs:63` — the Flow file is stored raw (never template-filled).

## Fix
In `engine/stages/codegen-schema.cjs`, replace the block from line 44 through line 69 with: