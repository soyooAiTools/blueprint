# auto-c7f2a915
## Diagnosis
`fidelity-source-diff` (canRetry:false) correctly detects 17 blocking entity-missing field-diffs and ≥1 pixel-gate failure, then throws at line 437 without first writing the missing entity names into `ctx.blueprint.feedbackHistory`. The canvas-black demote guard (lines 366–424) that would pass control to visual-check's recode loop requires **all** pixel-gate-failing phases to exceed `CANVAS_BLACK_DEMOTE_THRESHOLD_PERCENT` (default 95 %). Phases 2–5 render partial but wrong content (60–94 % pixel diff), so `allPhasesBlack` is false, the guard does not fire, and the hard block is thrown. With no entity-level feedback in the blueprint, each task-level retry runs codegen against the same prompt context, regenerates the same build, and hits the same gate identically — producing the 4-retry loop across 2 tasks.

## Root Cause
`engine/stages/fidelity-source-diff.cjs:437` — `throw new Error(...)` in the `if (hasBlockingDiff)` branch executes without injecting the computed `blockingEntityNames` into `ctx.blueprint.feedbackHistory`. The canvas-black demote guard at line 367 (`pixelGateFailures.every(f => f.percent > 95 %)`) also never fires because phases 2–5 have partial rendering with pixel diffs in the 60–94 % range.

## Fix

### Part 1 — `engine/stages/fidelity-source-diff.cjs` (insert feedback injection before the throw at line 437)

Locate the `if (hasBlockingDiff)` block (lines 426–441). Insert the following block **immediately before** the `throw new Error(...)` call: