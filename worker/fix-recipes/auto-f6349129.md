# auto-f6349129
## Diagnosis
`detectAssemblyContractViolations` (in `assembly-plan-contracts.cjs`) correctly identifies when a non-owning `extraFile` writes to an assembly-plan-owned state and emits `assembly-state-owner-mismatch`. `method-check` has in-memory auto-repair for every other contract violation class (`autoRepairForbiddenGenericApis`, `autoRepairDuplicateStateFields`, `autoRepairPlayerAliasDrift`, `autoRepairInvalidPoolLiterals`, `autoRepairPartialClassMismatch`), but has **no** corresponding repair for `assembly-state-owner-mismatch`. The stage therefore falls straight to feedback-push + codegen-checkpoint invalidation. Because moving state writes between partial class files is beyond what a single-shot codegen retry reliably achieves, the violation recurs identically on every retry, burning the full retry budget.

## Root Cause
`engine/stages/method-check.cjs` — `execute()` lines 888–902: the auto-repair chain is missing a call to handle `assembly-state-owner-mismatch` before `detectContractViolations` runs. The `fileMayWriteState` helper needed for line-level repair is already exported from `engine/assembly-plan-contracts.cjs` (line 462) but is never used for remediation.

## Fix

### `engine/stages/method-check.cjs`

**1. Add the new auto-repair function** (insert after `autoRepairPartialClassMismatch`, before the `execute` function, ~line 508):