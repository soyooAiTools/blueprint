# auto-5b4304a5
## Diagnosis
The LLM correctly generates `chineseName` and `showLabel` on every entity (as instructed in `buildSchemaPrompt`, rules 9a/9b). Before validation, `_repairSchema` iterates over every entity key and **deletes** anything not listed in `ALLOWED_ENTITY_KEYS`. Because `chineseName` and `showLabel` are absent from that allowlist they are silently stripped, leaving entities without the field that `game-schema.json` declares as `"required"`. Every attempt then fails with the same fingerprint, burning all 3 retries.

## Root Cause
`engine/stages/codegen-schema.cjs:291` — `ALLOWED_ENTITY_KEYS` whitelist is missing `chineseName` and `showLabel`: