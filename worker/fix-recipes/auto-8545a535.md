# auto-8545a535 — SUPERSEDED 2026-05-03

> ⚠️ This auto-generated recipe was produced by the auto-fix engine in
> parallel with a manual investigation. Its proposed value (480 s) was
> superseded by the actual fix in commit `d392da1` (600 s + `pipeline.cjs`
> FATAL short-circuit). The fingerprint `Timed out after 180000ms` can no
> longer be hit because the timeout is now 600000 ms. Removed from
> `fix-recipes.json` so the engine no longer matches it.
> See `docs/INCIDENTS.md` 2026-05-03 entry.

## Diagnosis
When the primary schema backend (codex-exec) fails or is in cooldown, `generateSchemaTextWithFallback` (codegen-schema.cjs:344) calls `runSchemaFallback`, which passes `timeoutMs: resolveSchemaFallbackTimeoutMs()` = **180 000 ms** into `runCodexText`. The `claude --print` subprocess for complex blueprint schema prompts consistently exceeds 3 minutes, so `runCodexText`'s internal `setTimeout` fires SIGTERM → exit 143. `isSchemaNonRetryableError` then matches the resulting `Timed out after 180000ms; Exit code 143` string and suppresses the inner retry loop, so all 3 pipeline-level stage retries fail identically, terminating the task.

## Root Cause
`.env:38` and `worker/.env:26` — `CODEX_SCHEMA_FALLBACK_TIMEOUT_MS=180000`

The value was introduced by recipe `auto-447aedf6` to "fail-fast", but 3 minutes is insufficient for the `claude --print` backend to stream and return a full schema JSON for complex blueprints under real network conditions. The downstream guard in `codegen-schema.cjs:453-456` (`isSchemaNonRetryableError`) intentionally blocks retries on timeout, so every attempt terminates after exactly 180 s without recovery.

Supporting call chain:
- `codegen-schema.cjs:353` → `timeoutMs: resolveSchemaFallbackTimeoutMs()` → `180000`
- `codex-code-coder.js:900-906` → `setTimeout(SIGTERM, 180000)` → exit 143
- `codex-code-coder.js:925` → `timedOut=true` → `"Timed out after 180000ms; Exit code 143"`
- `codegen-schema.cjs:292` → throws `"Schema generation failed: Timed out after 180000ms; Exit code 143"`
- `codegen-schema.cjs:296` → `isSchemaNonRetryableError` matches → no inner retry
- `pipeline.cjs:385-387` → stage `canRetry:true` burns all 3 outer retries → task fails

## Fix
In **both** `.env` files, raise the fallback timeout from 3 minutes to 8 minutes (still well under the original 10-minute hardcoded default, and 24 min total for 3 pipeline retries vs the former 48 min worst case):

**`/opt/blueprint-editor/.env` — line 38**