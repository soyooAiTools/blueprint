# auto-4395d1e1
## Diagnosis
When the game never reaches phase 1 in 2 consecutive CUA rounds, `_buildStuckDiagnosis` (line 69) merges the "never started" case into `visual_freeze` via the `noPhasesCompleted && noProgressRounds >= 2` guard. This fires **only** on round 2, meaning round 1's recode receives no actionable root-cause advice (rootCause stays `'unknown'`). The `visual_freeze` advice block talks about batch-completion timing and AutoPlayForceAdvance guards — both irrelevant when the game hasn't initialized at all. The round-1 recode therefore cannot target the real failure (Init/Start not running, pool objects off-screen, loadSettings null, etc.), the game still doesn't start on round 2, `_noProgressRounds >= 2` triggers the FATAL throw, the task fails, and the worker retries it 5× burning the same ineffective 2-round cycle each time.

## Root Cause
`engine/stages/cua-verify.cjs:69` — `noPhasesCompleted && noProgressRounds >= 2` condition (a) activates one round too late to give correct advice on round 1, and (b) collapses the distinct "game never initialized" failure mode into `visual_freeze`, which has entirely wrong remediation advice.

## Fix
Apply three targeted changes to `engine/stages/cua-verify.cjs`:

**Change 1 — Line 69: split `noPhasesCompleted` into its own root cause, fire from round 1**