# auto-7c563271
## Diagnosis
`_repairSchema` (line 1478 of `codegen-schema.cjs`) strips extra properties from every nested object — entities, phases, resources, triggers, actions — but has **no top-level cleanup pass**. The root `game-schema.json` declares `"additionalProperties": false`, so any extra key the LLM places at the top level (common LLM additions: `version`, `title`, `metadata`, `description`, `settings`, `$schema`) survives `_repairSchema` and causes AJV to throw `should NOT have additional properties`. The fallback `_repairSchemaValidationErrors` loop only handles `.phases[N] should NOT have additional properties`; it has no case for the root-level variant (AJV `dataPath = ''` → error string ` should NOT have additional properties` with a leading space).

## Root Cause
`engine/stages/codegen-schema.cjs:1479` — the guard clause in `_repairSchema` is immediately followed by nested-object cleanup loops, but there is no `Object.keys(schema).forEach(…)` call to strip unknown top-level keys before AJV validation is invoked at line 1741.

## Fix

**In `engine/stages/codegen-schema.cjs`**

### Change 1 — primary fix: strip extra top-level keys inside `_repairSchema`

Insert after line 1479 (`if (!schema || typeof schema !== 'object') return;`):