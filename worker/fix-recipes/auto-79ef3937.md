# auto-79ef3937
## Diagnosis
When the Claude Code CLI exits non-zero without modifying watched `.cs` files and `stderr` is empty, `runClaudeCode()` resolves with `error: stderr || \`Exit code ${code}\``.  Because `stderr` is empty, every fix-loop round in the `codegen` stage throws the exact same message `"AI coding failed: Claude Code failed (exit 1): Exit code 1"`.  `fix-loop.cjs` classifies this as a `CODE` error (it matches no INFRA/MODEL_FATAL pattern), computes an identical 63-char circuit-breaker signature on every round, and aborts after `sameErrorThreshold = 3` rounds without ever reading the actual stdout output that CC produced.

## Root Cause
`worker/claude-code-coder.js:475` — the error-string fallback in the `child.on('close')` resolve path ignores `stdout` entirely: