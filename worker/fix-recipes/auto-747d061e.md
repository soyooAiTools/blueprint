# auto-747d061e
## Diagnosis
`codegen-schema.cjs` calls `runCodexText` with a hard-coded `timeoutMs: 120000` (2 min). By default `runCodexText` passes `--tools Read` to the Codex text runner, which requires at least one extra tool-call API round-trip before the model produces the JSON schema. Under any load, 2 minutes is not enough; the timeout fires, sends SIGTERM (exit 143), and `runCodexText` returns `{ ok: false, error: 'Exit code 143' }`. `generateSchemaFromSpecs` then throws `"Schema generation failed: Exit code 143"`. All three retry attempts fail with the same timeout, surfacing the fingerprint error.

## Root Cause
`engine/stages/codegen-schema.cjs:86` — `timeoutMs: 120000` is set (2 min), and `noTools` is absent so `--tools Read` is passed, adding a tool-call round-trip. Schema generation is pure text output; it never needs to read any file in the workdir, so the `Read` tool is waste that can cause the extra latency that tips the call over the 2-minute limit.

## Fix
In `engine/stages/codegen-schema.cjs`, inside the `runCodexText` call:

**Before:**
