# auto-c526b422
## Diagnosis
This is not the >95% black-canvas/no-render signature; the pixel gap is around 68% and field blockers are concentrated in phase2 while the pixel gate fails from phase1 onward. That pattern indicates the target page is rendering, but fidelity extraction is reading a stale or mutated runtime phase after `__driveToPhase` settles. The Luna update loop can overwrite phase visibility/HUD/entity state during the settle window.

## Root Cause
worker/linux-bridge-build.js:4137

## Fix
In `worker/linux-bridge-build.js`, add a reusable helper immediately before `driveLoopComponentToPhase`: