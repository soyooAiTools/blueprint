{
  "schemaVersion": "1.2.0",
  "kind": "blueprint.fidelityContract.schema",
  "contractVersion": "1.2.0",
  "description": "Authoritative data contract for high-fidelity HTML and Unity reproduction. HTML writers and Unity writers consume generated blueprint.fidelityContract instances; runtime bridge infra must not carry fixture constants.",
  "instanceKind": "blueprint.fidelityContract",
  "acceptedInstanceSchemaVersions": ["1.0.0", "1.1.0", "1.2.0"],
  "versionPolicy": {
    "schemaVersion": "Version of this schema document. Bump on structural changes to this file.",
    "contractVersion": "Version of generated fidelityContract instance semantics. Bump when required roots, writer gates, or consumer semantics change.",
    "v1_1_0_notes": "Adds entities[].worldLabel (optional, polymorphic) and relaxes hud[].text to polymorphic. Polymorphic shape: string OR {default?: string, perPhase: Record<phaseId,string>}. Lib field-diff @0.6.1 reads v1.0.0 instances via transitional hud[id^=\"label.\"] fallback into worldLabel bucket; migration script scripts/migrate-v1.0-to-v1.1.cjs moves them forward.",
    "v1_2_0_notes": "Adds phases[].cameraTransform (informative) + phases[].projectedAnchors (normative, blocking) to make per-phase screen-space projection the canonical visual closure truth. Opt1 falsification (msg=ee995411) proved cameraTransform alone insufficient — Three.js↔PlayCanvas world coord/projection semantics are not equivalent. projectedAnchors carry source-observed 2D screen rects (viewport baseline 1280×720, origin top-left) so target writers calibrate via local screen-space fit instead of replaying source camera math. Lib field-diff @0.7.0 reads v1.1.0 instances via transitional fallback: projectedAnchors absent → unresolvedFidelityGaps[] advisory entry; cameraTransform absent → inferred-default. v1.1.0 instances continue to load. Migration script scripts/migrate-v1.1-to-v1.2.cjs is backfill-only: accepts 1.1.0 or 1.2.0 input, runs anchor-extractor against source HTML if available, fills projectedAnchors; if source HTML absent, leaves contract + unresolvedFidelityGaps advisory (NO fabrication). Idempotent. Conditional schemaVersion bump: bumps 1.1.0 → 1.2.0 only if every phase has projectedAnchors populated with provenance ∈ {extracted, anchor-only} (no inferred-default entries)."
  },
  "requiredRoots": [
    "schemaVersion",
    "kind",
    "producerVersion",
    "requiredCapabilities",
    "coordinateSystem",
    "rendererAdapter",
    "entities",
    "phases",
    "hud",
    "unityCoverage",
    "unresolvedFidelityGaps",
    "contractConflicts"
  ],
  "capabilityPolicy": {
    "field": "requiredCapabilities",
    "writerRule": "Every writer must declare supportedCapabilities. Missing support is a hard fail before asset/code emission.",
    "knownCapabilities": [
      "geometry.primitiveHierarchy.v1",
      "geometry.localTransform.v1",
      "geometry.pivotBounds.v1",
      "material.fullSurface.v1",
      "material.precomputedGuid.v1",
      "phase.behaviorGate.v1",
      "ui.labelAnchor.v1",
      "runtime.noFixtureBridgeConstants.v1",
      "rendererAdapter.driverMap.v1",
      "camera.perPhaseProjection.v1",
      "screen.perPhaseAnchors.v1",
      "provenance.fieldLevel.v1"
    ]
  },
  "coordinateSystem": {
    "description": "Coordinates are explicit because source Three.js RH and Unity LH differ.",
    "requiredKeys": [
      "source",
      "target",
      "handedness",
      "zFlip",
      "unitScale"
    ],
    "acceptedSources": [
      "three-rh",
      "unity-lh",
      "html-2d",
      "inferred"
    ],
    "acceptedTargets": [
      "three-rh",
      "unity-lh",
      "html-2d"
    ]
  },
  "rendererAdapter": {
    "description": "Driver-level mapping bucket. Fidelity data lives in the shared roots; unavoidable renderer/runtime mappings live here.",
    "requiredDrivers": [
      "three",
      "unity"
    ],
    "requiredBuckets": [
      "shader",
      "animator",
      "physics",
      "audio",
      "ui",
      "camera",
      "screen"
    ],
    "consumerRule": "Each generated contract field may declare consumer: [\"html\", \"unity\", \"report\"]. Driver-specific translation details stay under rendererAdapter.{three,unity}."
  },
  "provenance": {
    "description": "Field-level provenance for reverse extraction and conflict decisions.",
    "requiredKeys": [
      "source",
      "confidence"
    ],
    "acceptedSources": [
      "unity",
      "html",
      "spec",
      "inferred"
    ],
    "optionalKeys": [
      "assetPath",
      "guid",
      "fileID",
      "propertyPath",
      "line",
      "note"
    ]
  },
  "entityContract": {
    "idPolicy": "SourcePrimitive_<Entity>_<NN> is common but not required. Reverse extractors must also carry non-SourcePrimitive material families such as TargetRing_Mat, TrailLine_Mat, and LaserLine_Mat.",
    "requiredKeys": [
      "id",
      "name",
      "parentPath",
      "transform",
      "pivot",
      "bounds",
      "primitives",
      "provenance"
    ],
    "optionalKeys": [
      "label",
      "worldLabel",
      "unityPath",
      "htmlPath",
      "consumer"
    ],
    "worldLabelPolicy": {
      "shape": "string | {default?: string, perPhase: Record<phaseId,string>}",
      "semantics": "World-space label rendered on/over the entity in 3D (e.g. Three.js Sprite + DOM overlay in source HTML). Distinct from hud[] which is screen-space HUD overlay. Diff bucket = worldLabel-{missing,mismatch}; non-blocking advisory (HTML targets may legitimately render world labels off; Unity targets typically render them on).",
      "v1_0_migration": "v1.0.0 contracts carried world labels as hud[id=\"label.<EntityName>\"] entries with role:\"entity-label\". v1.1 migration moves them to entities[id=<EntityName>].worldLabel; orphans (no matching entity) remain in hud[] with unresolvedFidelityGaps[] warning."
    }
  },
  "sourceEntityContract": {
    "optional": true,
    "uiOverlayContract": {
      "shape": "{present:boolean, entities:Array<{id:string, role:string}>}",
      "semantics": "Screen-space UI scaffold discovered in the source HTML, such as CTA button, Canvas, floating joystick background and handle. These are not world-space entities and should not become entity-extra phantom blockers when a target extractor reports them through the runtime scene graph. They remain visible UI signals handled by HUD/visual checks.",
      "diffPolicy": "field-diff suppresses entity-extra for ids explicitly listed here only; this is not a generic runtime skip-list."
    }
  },
  "primitiveContract": {
    "requiredKeys": [
      "id",
      "name",
      "parentPath",
      "transform",
      "pivot",
      "bounds",
      "mesh",
      "material",
      "provenance"
    ],
    "meshRequiredKeys": [
      "type",
      "args"
    ],
    "materialRequiredKeys": [
      "id",
      "shader",
      "colors",
      "alpha",
      "blendMode",
      "guid",
      "guidSeed",
      "guidAlgorithm",
      "contentHash"
    ],
    "materialColorKeys": [
      "_Color",
      "_ColorTint",
      "_EmissionColor"
    ],
    "materialOptionalKeys": [
      "baseColor",
      "texture",
      "textureHash",
      "tiling",
      "offset",
      "renderQueue",
      "font",
      "fontSize",
      "alignment"
    ]
  },
  "phaseContract": {
    "requiredKeys": [
      "id",
      "showEntities",
      "trigger",
      "interactionGate",
      "autoPlayGate",
      "manualGate",
      "cameraTransform",
      "projectedAnchors"
    ],
    "description": "Behavior must be shifted left too: phase visibility, trigger evidence, and auto/manual gates are shared by HTML and Unity. v1.2 adds cameraTransform (informative) + projectedAnchors (normative, blocking) — see cameraTransformShape and projectedAnchorsShape.",
    "versionConditionalEnforcement": "cameraTransform + projectedAnchors are enforced only when schemaVersion >= 1.2.0. v1.0/v1.1 instances continue to validate without these keys (acceptedInstanceSchemaVersions covers backward-compat read path).",
    "cameraTransformShape": {
      "description": "Per-phase camera world-space pose. INFORMATIVE/DIAGNOSTIC ONLY — not blocking visual closure. Use cases: (1) writer initial camera setup, (2) diagnostic when projectedAnchors drift, (3) future renderer hint when projectedAnchors are absent. Renderer-agnostic.",
      "normativity": "informative",
      "requiredKeys": ["position", "lookAt", "fov", "projection"],
      "optionalKeys": ["up", "near", "far", "aspect", "provenance"],
      "shape": {
        "position": "vec3 — camera world position",
        "lookAt": "vec3 OR { mode: 'entity', entityId: string } OR { mode: 'expression', expr: string }",
        "fov": "number — field of view in degrees (perspective only)",
        "projection": "'perspective' | 'orthographic'",
        "up": "vec3 — default [0,1,0]",
        "near": "number — default 0.1",
        "far": "number — default 1000",
        "aspect": "number — default 'auto'",
        "provenance": "'extracted' | 'inferred-default'"
      }
    },
    "projectedAnchorsShape": {
      "description": "Per-phase, per-entity source-observed screen-space rect. NORMATIVE / BLOCKING. Captured by running source Three.js renderer + camera.project() against each visible entity's world AABB → 8-vertex screen projection → axis-aligned 2D bbox. Target writers consume these as ground-truth screen targets for local PlayCanvas/Luna screen-space calibration. Target writers MUST NOT iframe-replay source.",
      "normativity": "normative",
      "polymorphic": false,
      "polymorphism_note": "v1.2.0 first ship is flat-per-phase (Jonny msg=892495cc / Sam preference). Default/perPhase polymorphic resolver deferred — phases[] is already phase-scoped so flat captures the model. Revisit only if cross-viewport / cross-default semantics emerge.",
      "viewportBaseline": {
        "width": 1280,
        "height": 720,
        "origin": "top-left",
        "rationale": "Matches engine/stages/fidelity-source-diff.cjs DEFAULT_VIEWPORT={1280,720} + sharp resize/cover pixel-diff baseline + Luna/PlayCanvas DOM canvas convention. Top-left origin matches DOM/image-pixel y-down convention used by sharp + html2canvas downstream."
      },
      "shape": {
        "projectedAnchors": {
          "<entityId>": {
            "x_px": "number — screen-space x (pixels, top-left origin) [REQUIRED]",
            "y_px": "number — screen-space y (pixels, top-left origin) [REQUIRED]",
            "w_px": "number — bbox width in pixels [REQUIRED]",
            "h_px": "number — bbox height in pixels [REQUIRED]",
            "provenance": "'extracted' | 'inferred-default' | 'anchor-only' [REQUIRED]",
            "depth_ndc": "number OPTIONAL — center-z in NDC [-1, 1], for occlusion-order diagnostics",
            "lookupPath": "OPTIONAL audit — 'meshes' | 'scene.getObjectByName' | 'not-found'",
            "matchedAlias": "OPTIONAL audit — source Three.js .name actually matched",
            "resolverRule": "OPTIONAL audit — 'direct' | 'catalog-alias' | 'convention' | 'unresolved'"
          }
        }
      },
      "validatorRequiredKeys": ["x_px", "y_px", "w_px", "h_px", "provenance"],
      "validatorOptionalKeys": ["depth_ndc", "lookupPath", "matchedAlias", "resolverRule"],
      "validatorForbiddenKeys": ["selfVisible", "effectiveVisible", "viewportIntersection"],
      "validatorForbiddenKeysRationale": "Derived diagnostic booleans are PoC/migration-report-only. Writer and field-diff derive viewportIntersection at consume time from x_px/y_px/w_px/h_px via the bbox-rect ∩ viewport-rect formula against viewportBaseline. Persisting these would couple historical contracts to viewport constants and violate the v6.1 persistence boundary (Jonny msg=0ec2b725 / Tim msg=52fe75ad).",
      "provenanceAcceptedValues": ["extracted", "inferred-default", "anchor-only"],
      "bboxSourceRule": "source-side entity world AABB → 8 vertex × camera matrix → NDC → viewport transform → screen min/max → axis-aligned 2D rect. extractor MUST handle Three.js Object3D.matrixWorld + Camera.matrixWorldInverse + Camera.projectionMatrix in this order. for entities without geometry (e.g. labels), bbox = label DOM rect or fallback to anchor-only (w_px=0, h_px=0, provenance='anchor-only').",
      "showEntitiesCoupling": "phases[i].projectedAnchors MUST contain a key for every entityId in phases[i].showEntities (1:1 enforcement, per Jonny review point 2). missing key → schema VALIDATION blocking; present key with provenance='inferred-default' → unresolvedFidelityGaps[] advisory (writer still gets calibration target, just lower confidence).",
      "emptyShowEntitiesRule": "When showEntities[] is empty, projectedAnchors MUST still be present (it is in phaseContract.requiredKeys) but as an empty object {}. Validator accepts {} as trivially satisfying the 1:1 coupling. Omitting the projectedAnchors key entirely fails validation in v1.2.0+ instances (per Tim msg=7caa6ec8 clarification, locked).",
      "tolerance": "writer-side anchor diff: ±8px absolute on x_px/y_px/w_px/h_px (configurable via fidelity-source-diff stage env DEFAULT_ANCHOR_PIXEL_TOLERANCE). Beyond tolerance → blocking field-diff entry. Exception: when expected anchor's derived viewportIntersection=false (source-positioned-offscreen), anchor-mismatch downgrades to advisory unresolvedFidelityGaps[] with kind='anchor-mismatch-off-viewport', NOT hasBlockingDiff (Jonny msg=0ec2b725 / Tim msg=52fe75ad). Pixel-gate hard <5% covers off-canvas regions via sharp resize/cover.",
      "consumerDerivedViewportIntersection": "viewportIntersection = (x_px + w_px) >= 0 AND x_px <= viewportBaseline.width AND (y_px + h_px) >= 0 AND y_px <= viewportBaseline.height. Bbox-rect ∩ viewport-rect (NOT center-point). Derived in-memory at consume time by writer and field-diff; NEVER persisted in contract."
    }
  },
  "hudContract": {
    "requiredKeys": [
      "id",
      "text",
      "anchor",
      "consumer",
      "provenance"
    ],
    "textShape": "string | {default?: string, perPhase: Record<phaseId,string>}",
    "textResolverPolicy": "Lib field-diff @0.6.1 resolution order: perPhase[phaseId] > default > undefined (skip diff for this phase). Q3 tolerance: expected '' and observed undefined treated as match.",
    "rolePolicy": "role:\"entity-label\" + id:\"label.<EntityName>\" is the v1.0.0 transitional shape for world-space labels; v1.1 forward expresses these as entities[].worldLabel and treats them as a separate diff bucket."
  },
  "unityCoverage": {
    "acceptedStatus": [
      "complete",
      "partial",
      "missing"
    ],
    "rule": "Pure HTML fixtures may use missing, but Unity writers must fail unless explicitly allowed during migration."
  },
  "writerFailFast": {
    "unresolvedFidelityGaps": "Any gap with blocking !== false blocks writer emission.",
    "contractConflicts": "Any conflict without resolution.status=accepted|rejected|deferred blocks writer emission.",
    "fallbackPolicy": [
      "No CreatePrimitive fallback",
      "No phantom entities",
      "Delivery__*.mat entries must be recorded as unresolvedFidelityGaps unless intentionally mapped with provenance and resolution",
      "No legacy Delivery__*.mat material fallback",
      "No silent default material",
      "No fixture constants inside Luna bridge/runtime infra"
    ]
  },
  "roundTripPolicy": {
    "description": "HTML/Unity writer readback must diff against the source contract before final delivery.",
    "hardGate": "Readback diff must be zero for required data roots. Visual screenshot/VLM diff is advisory after this gate."
  }
}
